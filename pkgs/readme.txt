fetch sha using


nix run nixpkgs#nix-prefetch-git repo-url

add pkg in home/nix/home.nix if we just want to install as package


next steps are if we want a whole nixos module for that packages

create module in modules... examples are placed

import that in flake.nix home manager module
